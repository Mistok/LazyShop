include header

div(class='wrapper')
    div(class='content')
        header(class='site-header')
            include site_header

        div(class='container')
            div(class='row')
                div(class='col-12 col-md-12 mb-4')
                    h2 #{cat[0]['category']}
                each val, index in goods
                    include card



                div #{cat[0]['description']}

    include nav.pug
    include footer.pug

